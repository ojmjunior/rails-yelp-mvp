<div class="container">
  <H1>NEW REVIEW</H1>
  
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Save review", class: "btn btn-primary"%>
  <% end %>
  
</div>