<div class="container">
  <h2>VIEW DA INDEX DO RESTAURANTS</h2>
  
  <% @restaurants.each do |restaurant| %>
  <li><%= link_to "#{restaurant.name}", restaurant_path(restaurant) %></li>
  <% end %>
  
  <%= link_to '+ Add a restaurant', '/restaurants/new' %>
</div>